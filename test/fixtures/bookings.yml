# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

one:
  id: 1
  student_id: students(:one).student_id
  computer_id: computers(:one).computer_id
  timeunit_id: timeunits(:one).timeunit_id
  date: 2010-12-26

two:
  id: 2
  student_id: students(:two).student_id
  computer_id: computers(:two).computer_id
  timeunit_id: timeunits(:two).timeunit_id
  date: 2010-12-25

# == Schema Information
#
# Table name: bookings
#
#  id          :integer         not null, primary key
#  student_id  :integer
#  computer_id :integer
#  timeunit_id :integer
#  date        :date
#  created_at  :datetime
#  updated_at  :datetime
#

